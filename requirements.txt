# Web Framework
Flask==2.3.3
Werkzeug==2.3.7
gunicorn==21.2.0
python-dotenv>=1.0.0

# Data Processing
pandas==2.2.3
openpyxl==3.1.5

# PDF Processing
pdfplumber==0.9.0
pdf2image==1.17.0
pymupdf==1.28.2

# Image Processing
Pillow==11.3.0
opencv-python==4.8.1.78
numpy==1.26.4

# OCR - Tesseract (legacy fallback)
pytesseract==0.3.13

# OCR - RapidOCR ONNX primary; Tesseract fallback
rapidocr-onnxruntime==1.4.4
onnxruntime==1.22.1

# The public layout-preserving pipeline does not install img2table. The dormant
# structured parser treats it as optional and falls back when it is unavailable.

# LLM Integration - OpenAI
openai>=1.0.0
httpx>=0.25.0
anthropic>=0.39.0

# Email
resend>=2.0.0

# Payments
stripe>=7.0.0

# Rate Limiting (for SaaS)
flask-limiter>=3.5.0
redis>=5.0.0

# Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9.0

# Async Task Queue
celery==5.3.6

# Object storage
boto3>=1.34.0

# Testing & Generation
pytest>=8.0.0
Faker>=23.0.0
reportlab>=4.0.0
pdf2image>=1.17.0
# Pillow is already listed above under Image Processing
